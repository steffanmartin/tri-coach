__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

.env

# local clone of the vault repo (see VAULT_PATH in README/.env.example)
/vault/
/triathlon-brain/

.DS_Store

# Git worktrees created by Claude Code sessions. Full second checkouts;
# .dockerignore excludes them from the image for the same reason.
.claude/worktrees/
